# Tunario

A simple progressive web app (PWA) for managing music resources. You can upload setlists and sheet music and view a basic dashboard.

## Setup

Install dependencies and start the server:

```bash
npm install
npm start
```

The app will run at `http://localhost:3000`. Open this URL in your browser to use the PWA. The service worker and manifest allow you to install the app on your device.
